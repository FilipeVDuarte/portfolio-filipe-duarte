{
  "name": "portfolio-filipe-duarte",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build:css": "npx lightningcss --minify --targets \">= 0.5%\" public/css/style.css -o public/css/style.min.css",
    "build": "npm run build:css && astro build",
    "preview": "astro preview"
  },
  "devDependencies": {
    "lightningcss-cli": "^1.29.2"
  },
  "dependencies": {
    "astro": "^6.4.2"
  }
}
