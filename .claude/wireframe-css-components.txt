=== melhorar-portfolio/project/wireframe.css ===
/* =========================================================
   Portfolio refactor — low-fi sketchy wireframe styles
   ========================================================= */

:root {
  --ink: #1a1a1a;
  --ink-soft: #3a3a3a;
  --muted: #8a8a8a;
  --paper: #f6f4ef;
  --paper-2: #ecead7;
  --grid: #d9d5c6;
  --accent: #ffc300;  /* DS yellow, used sparingly */
  --accent-2: #ff2d55; /* DS pink, only on critical hot-spots */
  --note: #1f6feb;     /* annotation blue */

  --hand: "Kalam", "Caveat", system-ui, sans-serif;
  --hand-display: "Caveat", "Kalam", system-ui, sans-serif;
  --sans: "Inter", system-ui, sans-serif;
  --mono: "Space Mono", ui-monospace, monospace;

  --stroke: 1.5px solid var(--ink);
  --stroke-dash: 1.5px dashed var(--ink);
  --stroke-thick: 2.5px solid var(--ink);

  --gap-1: 6px;
  --gap-2: 12px;
  --gap-3: 18px;
  --gap-4: 28px;
  --gap-5: 44px;
  --gap-6: 64px;

  --radius: 3px;
}

*,*::before,*::after { box-sizing: border-box; }

html, body {
  margin: 0;
  background: var(--paper);
  color: var(--ink);
  font-family: var(--hand);
  font-weight: 400;
}

body {
  /* faint dotted graph paper */
  background-image:
    radial-gradient(circle, rgba(0,0,0,0.07) 1px, transparent 1.2px);
  background-size: 22px 22px;
  background-position: -11px -11px;
  min-height: 100vh;
}

img { max-width: 100%; display: block; }
button { font-family: inherit; }

/* ─── App shell ─── */
.app {
  max-width: 1240px;
  margin: 0 auto;
  padding: 22px 28px 80px;
}

.app__hdr {
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
  gap: 24px;
  padding-bottom: 14px;
  margin-bottom: 24px;
  border-bottom: var(--stroke);
}
.app__hdr h1 {
  font-family: var(--hand-display);
  font-size: 32px;
  font-weight: 700;
  line-height: 1.1;
  margin: 0;
}
.app__hdr h1 + small,
.app__hdr small { margin-top: 8px; }
.app__hdr small {
  font-family: var(--mono);
  font-size: 11px;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: var(--muted);
  display: block;
  margin-top: 6px;
}
.app__route {
  font-family: var(--mono);
  font-size: 10.5px;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: var(--muted);
  text-align: right;
}
.app__route b { color: var(--ink); }

/* ─── Page tabs (router) ─── */
.tabs {
  display: flex;
  gap: 0;
  border: var(--stroke);
  border-radius: var(--radius);
  background: var(--paper);
  width: fit-content;
  margin-bottom: var(--gap-4);
  overflow: hidden;
}
.tabs__btn {
  appearance: none;
  background: transparent;
  border: 0;
  border-right: var(--stroke);
  padding: 10px 16px;
  font-family: var(--mono);
  font-size: 11px;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: var(--ink);
  cursor: pointer;
}
.tabs__btn:last-child { border-right: 0; }
.tabs__btn[aria-current="true"] { background: var(--ink); color: var(--paper); }
.tabs__btn:not([aria-current="true"]):hover { background: var(--paper-2); }
.tabs__variant {
  margin-left: auto;
  display: flex;
  align-items: center;
  gap: 8px;
  font-family: var(--mono);
  font-size: 10.5px;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: var(--muted);
}
.tabs-row {
  display: flex;
  align-items: center;
  gap: 18px;
  margin-bottom: var(--gap-4);
  flex-wrap: wrap;
}

.var-pills {
  display: flex;
  gap: 6px;
}
.var-pills button {
  appearance: none;
  background: transparent;
  border: var(--stroke);
  border-radius: var(--radius);
  padding: 6px 12px;
  font-family: var(--mono);
  font-size: 10.5px;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  cursor: pointer;
}
.var-pills button[aria-pressed="true"] {
  background: var(--accent);
}

/* ─── Browser frame ─── */
.frame {
  border: var(--stroke-thick);
  border-radius: 6px;
  background: var(--paper);
  box-shadow: 5px 5px 0 var(--ink);
  overflow: hidden;
  margin-bottom: var(--gap-5);
}
.frame__bar {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 8px 14px;
  border-bottom: var(--stroke);
  background: var(--paper-2);
  font-family: var(--mono);
  font-size: 10.5px;
  letter-spacing: 0.06em;
}
.frame__bar .dot {
  width: 9px; height: 9px;
  border: 1.2px solid var(--ink);
  border-radius: 50%;
}
.frame__bar .url {
  margin-left: 8px;
  padding: 3px 10px;
  border: var(--stroke);
  border-radius: 999px;
  background: var(--paper);
  color: var(--ink-soft);
  flex: 1;
  max-width: 480px;
}
.frame__body { padding: 0; }

/* ─── Page (inside browser frame) ─── */
.page {
  padding: 22px 28px 36px;
  position: relative;
}

/* ─── Sketchy wireframe primitives ─── */
.wf-nav {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  padding: 12px 0 14px;
  border-bottom: var(--stroke);
  margin-bottom: var(--gap-4);
}
.wf-nav__logo {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  font-family: var(--hand-display);
  font-weight: 700;
  font-size: 22px;
}
.wf-nav__logo .badge {
  display: inline-grid;
  place-items: center;
  width: 26px; height: 26px;
  background: var(--ink);
  color: var(--accent);
  font-family: var(--sans);
  font-weight: 700;
  font-size: 12px;
  border-radius: 2px;
}
.wf-nav__links {
  display: flex;
  gap: 22px;
  font-family: var(--mono);
  font-size: 11px;
  letter-spacing: 0.1em;
  text-transform: uppercase;
}
.wf-nav__links a { color: inherit; text-decoration: none; cursor: pointer; }
.wf-nav__links a.active { border-bottom: 2px solid var(--ink); padding-bottom: 2px; }
.wf-nav__tools {
  display: flex;
  gap: 8px;
  font-family: var(--mono);
  font-size: 11px;
  letter-spacing: 0.1em;
  text-transform: uppercase;
}
.wf-nav__tools span {
  display: inline-grid;
  place-items: center;
  min-width: 30px;
  height: 28px;
  padding: 0 8px;
  border: var(--stroke);
  border-radius: var(--radius);
}

/* ─── Hand-drawn box (image / placeholder) ─── */
.box {
  position: relative;
  border: var(--stroke-dash);
  border-radius: var(--radius);
  background:
    repeating-linear-gradient(
      135deg,
      transparent 0,
      transparent 12px,
      rgba(0,0,0,0.04) 12px,
      rgba(0,0,0,0.04) 13px
    );
  display: grid;
  place-items: center;
  font-family: var(--mono);
  font-size: 10.5px;
  letter-spacing: 0.1em;
  color: var(--muted);
  text-transform: uppercase;
  text-align: center;
  padding: 16px;
  overflow: hidden;
  min-height: 60px;
}
.box.solid {
  background: var(--paper-2);
  border-style: solid;
}
.box.ink {
  background: var(--ink);
  color: var(--paper);
  border-color: var(--ink);
}
.box.accent {
  background: var(--accent);
  border-style: solid;
  color: var(--ink);
}
.box.tall { min-height: 220px; }
.box.med { min-height: 160px; }
.box.short { min-height: 90px; }
.box .label {
  font-family: var(--mono);
  font-size: 10.5px;
  letter-spacing: 0.1em;
}
.box .sub {
  display: block;
  margin-top: 4px;
  font-family: var(--hand);
  font-size: 13px;
  letter-spacing: 0;
  text-transform: none;
  color: var(--ink-soft);
}

/* X mark over image placeholders (architectural blueprint style) */
.box.with-x::before, .box.with-x::after {
  content: "";
  position: absolute;
  inset: 0;
  pointer-events: none;
}
.box.with-x::before {
  background: linear-gradient(to top right, transparent calc(50% - 0.7px), rgba(0,0,0,0.18) 50%, transparent calc(50% + 0.7px));
}
.box.with-x::after {
  background: linear-gradient(to bottom right, transparent calc(50% - 0.7px), rgba(0,0,0,0.18) 50%, transparent calc(50% + 0.7px));
}

.box.with-x > * { position: relative; z-index: 1; background: var(--paper); padding: 4px 8px; }

/* Aspect ratio helpers */
.ar-16-9 { aspect-ratio: 16/9; min-height: 0; }
.ar-4-3 { aspect-ratio: 4/3; min-height: 0; }
.ar-1-1 { aspect-ratio: 1/1; min-height: 0; }
.ar-3-4 { aspect-ratio: 3/4; min-height: 0; }
.ar-21-9 { aspect-ratio: 21/9; min-height: 0; }

/* ─── Headline / type placeholders ─── */
.hl {
  font-family: var(--hand-display);
  font-weight: 700;
  line-height: 0.95;
  letter-spacing: -0.02em;
  margin: 0;
}
.hl.huge   { font-size: clamp(48px, 8vw, 112px); }
.hl.xl     { font-size: clamp(36px, 5.4vw, 72px); }
.hl.lg     { font-size: clamp(28px, 4vw, 52px); }
.hl.md     { font-size: clamp(22px, 2.6vw, 34px); }
.hl.outline{
  -webkit-text-stroke: 1.5px var(--ink);
  color: transparent;
}

.eyebrow {
  font-family: var(--mono);
  font-size: 11px;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: var(--muted);
  display: inline-flex;
  align-items: center;
  gap: 10px;
}
.eyebrow::before {
  content: "";
  width: 28px;
  height: 1.5px;
  background: var(--ink);
}

.lorem {
  font-family: var(--hand);
  font-size: 15px;
  line-height: 1.55;
  color: var(--ink-soft);
  max-width: 56ch;
}
.lorem.tight { line-height: 1.4; }
.lorem strong { color: var(--ink); font-weight: 700; }

/* Faux text lines */
.lines { display: flex; flex-direction: column; gap: 7px; }
.lines > i {
  display: block;
  height: 9px;
  background: var(--ink-soft);
  opacity: 0.18;
  border-radius: 2px;
}
.lines > i.w-95 { width: 95%; }
.lines > i.w-90 { width: 90%; }
.lines > i.w-80 { width: 80%; }
.lines > i.w-70 { width: 70%; }
.lines > i.w-60 { width: 60%; }
.lines > i.w-50 { width: 50%; }
.lines > i.w-40 { width: 40%; }
.lines > i.w-30 { width: 30%; }

/* ─── Buttons ─── */
.wf-btn {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  border: var(--stroke);
  background: var(--paper);
  padding: 10px 18px;
  border-radius: var(--radius);
  font-family: var(--mono);
  font-size: 11px;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  box-shadow: 3px 3px 0 var(--ink);
  cursor: pointer;
}
.wf-btn.primary { background: var(--ink); color: var(--paper); }
.wf-btn.accent  { background: var(--accent); }

/* ─── Pills / tags ─── */
.pill {
  display: inline-flex;
  align-items: center;
  padding: 4px 10px;
  border: var(--stroke);
  border-radius: 999px;
  font-family: var(--mono);
  font-size: 10.5px;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}
.pill.fill-y { background: var(--accent); }
.pill.fill-t { background: #b8efe9; }
.pill.fill-p { background: #ffd0da; }
.pill.fill-v { background: #ddd5ff; }
.pill.ink   { background: var(--ink); color: var(--paper); }

.pill-row { display: flex; flex-wrap: wrap; gap: 6px; }

/* ─── Stamp (rotated badge for 9,56/10) ─── */
.stamp {
  display: inline-grid;
  place-items: center;
  text-align: center;
  border: var(--stroke-thick);
  border-radius: var(--radius);
  padding: 12px 16px;
  background: var(--paper);
  box-shadow: 4px 4px 0 var(--ink);
  transform: rotate(-3deg);
  font-family: var(--hand-display);
  line-height: 1;
}
.stamp .num {
  font-size: 38px;
  font-weight: 700;
  letter-spacing: -0.02em;
}
.stamp .meta {
  display: block;
  margin-top: 4px;
  font-family: var(--mono);
  font-size: 9.5px;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: var(--muted);
}
.stamp.tilt-r { transform: rotate(3deg); }

/* ─── Grids ─── */
.row { display: flex; gap: var(--gap-3); align-items: stretch; flex-wrap: wrap; }
.row > * { min-width: 0; }
.col   { display: flex; flex-direction: column; gap: var(--gap-3); }
.grid-2 { display: grid; grid-template-columns: 1fr 1fr; gap: var(--gap-3); }
.grid-3 { display: grid; grid-template-columns: repeat(3, 1fr); gap: var(--gap-3); }
.grid-4 { display: grid; grid-template-columns: repeat(4, 1fr); gap: var(--gap-3); }
.grid-6 { display: grid; grid-template-columns: repeat(6, 1fr); gap: var(--gap-2); }
.grid-12{ display: grid; grid-template-columns: repeat(12, 1fr); gap: var(--gap-3); }

.hero-split { display: grid; grid-template-columns: 1.1fr 0.9fr; gap: var(--gap-4); align-items: center; }

.section { margin-top: var(--gap-5); position: relative; }
.section + .section { margin-top: var(--gap-6); }
.section__hdr {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
  gap: 16px;
  margin-bottom: var(--gap-3);
  padding-bottom: 8px;
  border-bottom: var(--stroke);
}
.section__hdr h2 { margin: 0; }

/* ─── Marquee (CSS infinite) ─── */
.marquee {
  border-top: var(--stroke);
  border-bottom: var(--stroke);
  padding: 18px 0;
  overflow: hidden;
  position: relative;
}
.marquee__track {
  display: flex;
  gap: 28px;
  width: max-content;
  animation: mq 28s linear infinite;
}
.marquee__track:hover { animation-play-state: paused; }
.marquee__item {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 10px 18px;
  border: var(--stroke);
  border-radius: var(--radius);
  font-family: var(--mono);
  font-size: 12px;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  background: var(--paper);
  white-space: nowrap;
}
.marquee__item .logo {
  width: 22px; height: 22px;
  display: inline-grid; place-items: center;
  font-family: var(--sans);
  font-weight: 700;
  font-size: 11px;
  border: 1.2px solid var(--ink);
  border-radius: 2px;
}
.marquee.compact { padding: 10px 0; }
.marquee.compact .marquee__item { padding: 6px 12px; font-size: 10.5px; }
.marquee.compact .marquee__item .logo { width: 18px; height: 18px; font-size: 9px; }
.marquee.big .marquee__item { padding: 14px 22px; font-size: 14px; }
.marquee.big .marquee__item .logo { width: 28px; height: 28px; font-size: 13px; }
@keyframes mq {
  from { transform: translateX(0); }
  to   { transform: translateX(-50%); }
}

/* ─── Cards ─── */
.card {
  border: var(--stroke);
  border-radius: var(--radius);
  background: var(--paper);
  padding: 16px;
  display: flex;
  flex-direction: column;
  gap: var(--gap-2);
}
.card.lift { box-shadow: 3px 3px 0 var(--ink); }
.card.dark { background: var(--ink); color: var(--paper); border-color: var(--ink); }
.card.accent { background: var(--accent); }

.card__title {
  font-family: var(--hand-display);
  font-weight: 700;
  font-size: 22px;
  line-height: 1.05;
}
.card__meta {
  font-family: var(--mono);
  font-size: 10.5px;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: var(--muted);
}

/* ─── Status dot ─── */
.status {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  font-family: var(--mono);
  font-size: 10.5px;
  letter-spacing: 0.1em;
  text-transform: uppercase;
}
.status::before {
  content: "";
  width: 8px; height: 8px;
  border-radius: 50%;
  background: #00bfae;
  box-shadow: 0 0 0 0 #00bfae66;
  animation: pulse 1.8s ease-in-out infinite;
}
@keyframes pulse {
  0%,100% { box-shadow: 0 0 0 0 #00bfae66; }
  50%     { box-shadow: 0 0 0 8px #00bfae00; }
}

/* ─── Filter bar ─── */
.filters {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  padding: 10px 0;
  border-top: var(--stroke);
  border-bottom: var(--stroke);
  margin-bottom: var(--gap-4);
}
.filters button {
  appearance: none;
  background: transparent;
  border: var(--stroke);
  border-radius: 999px;
  padding: 6px 14px;
  font-family: var(--mono);
  font-size: 11px;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  cursor: pointer;
}
.filters button[aria-pressed="true"] { background: var(--ink); color: var(--paper); }

/* ─── Footer ─── */
.wf-footer {
  margin-top: var(--gap-5);
  padding: 20px 0 4px;
  border-top: var(--stroke);
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: var(--gap-4);
  align-items: end;
}
.wf-footer .contacts {
  display: flex;
  flex-direction: column;
  gap: 6px;
  font-family: var(--mono);
  font-size: 11px;
  letter-spacing: 0.06em;
  text-transform: uppercase;
}
.wf-footer .contacts b {
  font-family: var(--hand-display);
  font-size: 18px;
  font-weight: 700;
  letter-spacing: -0.01em;
  text-transform: none;
  margin-bottom: 4px;
}
.wf-footer .mono-list {
  display: flex;
  gap: 16px;
  flex-wrap: wrap;
}

/* ─── Code block (case) ─── */
.code {
  background: var(--ink);
  color: var(--paper);
  border-radius: var(--radius);
  padding: 14px 16px;
  font-family: var(--mono);
  font-size: 12px;
  line-height: 1.6;
  white-space: pre;
  overflow: auto;
}
.code .c { color: #8a8a8a; }
.code .k { color: #ffd64d; }
.code .s { color: #b8efe9; }

/* ─── Timeline ─── */
.timeline {
  position: relative;
  padding-left: 30px;
}
.timeline::before {
  content: "";
  position: absolute;
  left: 8px; top: 4px; bottom: 4px;
  width: 1.5px;
  background: var(--ink);
}
.timeline__item {
  position: relative;
  padding-bottom: 28px;
}
.timeline__item::before {
  content: "";
  position: absolute;
  left: -27px;
  top: 6px;
  width: 12px; height: 12px;
  background: var(--paper);
  border: 1.5px solid var(--ink);
  border-radius: 50%;
}
.timeline__item.dot-fill::before { background: var(--ink); }
.timeline__year {
  font-family: var(--mono);
  font-size: 10.5px;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: var(--muted);
}
.timeline__title {
  font-family: var(--hand-display);
  font-weight: 700;
  font-size: 22px;
  line-height: 1.1;
  margin: 2px 0 6px;
}

/* ─── Annotation ─── */
.anno {
  position: absolute;
  font-family: var(--hand);
  font-size: 13px;
  line-height: 1.25;
  color: var(--note);
  max-width: 200px;
  pointer-events: none;
  z-index: 5;
}
.anno b { font-weight: 700; }
.anno svg {
  position: absolute;
  overflow: visible;
  pointer-events: none;
}

body[data-anno="off"] .anno { display: none; }

/* ─── Density ─── */
body[data-density="compact"] {
  --gap-3: 12px;
  --gap-4: 18px;
  --gap-5: 28px;
  --gap-6: 40px;
}
body[data-density="comfy"] {
  --gap-3: 24px;
  --gap-4: 36px;
  --gap-5: 56px;
  --gap-6: 80px;
}

/* ─── Utility ─── */
.flex { display: flex; }
.flex.aic { align-items: center; }
.flex.gap-1 { gap: 6px; }
.flex.gap-2 { gap: 12px; }
.flex.gap-3 { gap: 18px; }
.flex.gap-4 { gap: 28px; }
.flex.wrap { flex-wrap: wrap; }
.flex.col { flex-direction: column; }
.mt-1 { margin-top: 6px; } .mt-2 { margin-top: 12px; }
.mt-3 { margin-top: 18px; } .mt-4 { margin-top: 28px; }
.mb-2 { margin-bottom: 12px; } .mb-3 { margin-bottom: 18px; }
.grow { flex: 1; }
.center { text-align: center; }
.muted { color: var(--muted); }
.mono  { font-family: var(--mono); font-size: 11px; letter-spacing: 0.08em; text-transform: uppercase; }

/* ─── Page summary (top of each page block) ─── */
.page-summary {
  font-family: var(--hand);
  font-size: 14px;
  color: var(--ink-soft);
  padding: 8px 14px;
  margin-bottom: 18px;
  border: 1.5px dashed var(--note);
  border-radius: var(--radius);
  background: rgba(31,111,235,0.04);
  display: flex;
  gap: 14px;
  align-items: baseline;
}
.page-summary b {
  font-family: var(--mono);
  font-size: 10.5px;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: var(--note);
}
body[data-anno="off"] .page-summary { display: none; }

/* ─── Lottie hero placeholder ─── */
.lottie-hero {
  position: relative;
  border: 1.5px dashed var(--ink);
  border-radius: var(--radius);
  background:
    repeating-linear-gradient(
      45deg,
      transparent 0,
      transparent 14px,
      rgba(0,0,0,0.035) 14px,
      rgba(0,0,0,0.035) 15px
    );
  padding: 28px 32px;
  min-height: clamp(180px, 22vw, 320px);
  display: grid;
  place-items: center;
  overflow: hidden;
}
.lottie-hero__corners i {
  position: absolute;
  width: 16px; height: 16px;
  border: 1.5px solid var(--ink);
}
.lottie-hero__corners i:nth-child(1) { top: 8px; left: 8px; border-right: 0; border-bottom: 0; }
.lottie-hero__corners i:nth-child(2) { top: 8px; right: 8px; border-left: 0; border-bottom: 0; }
.lottie-hero__corners i:nth-child(3) { bottom: 8px; left: 8px; border-right: 0; border-top: 0; }
.lottie-hero__corners i:nth-child(4) { bottom: 8px; right: 8px; border-left: 0; border-top: 0; }
.lottie-hero__inner {
  text-align: center;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 10px;
}
.lottie-hero__icon {
  width: 56px; height: 56px;
  display: grid; place-items: center;
  border: 2px solid var(--ink);
  border-radius: 50%;
  font-size: 22px;
  background: var(--accent);
  animation: lottie-bob 2.4s ease-in-out infinite;
}
@keyframes lottie-bob {
  0%,100% { transform: translateY(0) scale(1); }
  50%     { transform: translateY(-6px) scale(1.05); }
}
.lottie-hero__label {
  font-family: var(--mono);
  font-size: 12px;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: var(--ink);
}
.lottie-hero__sub {
  font-family: var(--hand);
  font-size: 14px;
  color: var(--muted);
  line-height: 1.35;
}

/* ─── Certifications carousel ─── */
.carousel {
  position: relative;
}
.carousel__track {
  display: grid;
  grid-auto-flow: column;
  grid-auto-columns: calc((100% - var(--gap-3) * 2) / 3);
  gap: var(--gap-3);
  overflow-x: auto;
  scroll-snap-type: x mandatory;
  padding: 4px 4px 14px;
  scrollbar-width: thin;
}
.carousel__track > * { scroll-snap-align: start; }
.carousel__nav {
  display: flex;
  gap: 8px;
  align-items: center;
  margin-top: 4px;
}
.carousel__btn {
  appearance: none;
  background: var(--paper);
  border: var(--stroke);
  border-radius: var(--radius);
  width: 36px; height: 32px;
  font-family: var(--mono);
  cursor: pointer;
  box-shadow: 2px 2px 0 var(--ink);
}
.carousel__btn:hover { background: var(--accent); }
.carousel__dots {
  display: flex;
  gap: 6px;
  margin-left: auto;
}
.carousel__dots i {
  width: 24px; height: 4px;
  background: var(--ink-soft);
  opacity: 0.3;
  border-radius: 2px;
}
.carousel__dots i.on { opacity: 1; }

/* ─── Skill chip with icon (Competências) ─── */
.skill-chip {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  border: var(--stroke);
  border-radius: var(--radius);
  padding: 8px 12px;
  background: var(--paper);
  font-family: var(--mono);
  font-size: 11px;
  letter-spacing: 0.06em;
  text-transform: uppercase;
}
.skill-chip .logo {
  width: 22px; height: 22px;
  display: inline-grid; place-items: center;
  border: 1.2px solid var(--ink);
  border-radius: 2px;
  font-family: var(--sans);
  font-weight: 700;
  font-size: 10.5px;
  background: var(--paper);
}

/* ─── Responsiveness ─── */
@media (max-width: 900px) {
  .hero-split { grid-template-columns: 1fr; }
  .grid-3, .grid-4 { grid-template-columns: repeat(2, 1fr); }
  .grid-6 { grid-template-columns: repeat(3, 1fr); }
  .wf-footer { grid-template-columns: 1fr; }
}


=== melhorar-portfolio/project/components.jsx ===
// components.jsx — shared wireframe primitives (Nav, Footer, Box, Annotation)

const Nav = ({ active = "home", onNav }) => (
  <nav className="wf-nav">
    <a className="wf-nav__logo" onClick={() => onNav?.("home")} style={{cursor:'pointer'}}>
      <span className="badge">FD</span>
      Filipe Duarte
    </a>
    <div className="wf-nav__links">
      <a className={active === "home" ? "active" : ""} onClick={() => onNav?.("home")}>Home</a>
      <a className={active === "projetos" ? "active" : ""} onClick={() => onNav?.("projetos")}>Projetos</a>
      <a className={active === "sobre" ? "active" : ""} onClick={() => onNav?.("sobre")}>Sobre</a>
    </div>
    <div className="wf-nav__tools">
      <span title="Idioma">EN</span>
      <span title="Tema">◐</span>
    </div>
  </nav>
);

const Footer = () => (
  <footer className="wf-footer">
    <div className="contacts">
      <b>Vamos conversar.</b>
      <span>filipe@email.com</span>
      <span>+55 51 9 0000 0000  ·  WhatsApp</span>
    </div>
    <div style={{display:'flex', flexDirection:'column', gap:8, alignItems:'flex-end'}}>
      <div className="mono-list mono">
        <span>LinkedIn ↗</span>
        <span>Behance ↗</span>
        <span>GitHub ↗</span>
      </div>
      <div className="mono muted">© 2026 · Filipe V. Duarte Delfino</div>
    </div>
  </footer>
);

// Sketchy placeholder box
const Box = ({ label, sub, kind = "", ar = "", withX = false, style, children, className = "" }) => {
  const cls = ["box", kind, ar, withX ? "with-x" : "", className].filter(Boolean).join(" ");
  return (
    <div className={cls} style={style}>
      {label ? <div className="label">{label}{sub ? <span className="sub">{sub}</span> : null}</div> : children}
    </div>
  );
};

// Faux text block (fake paragraph)
const Lines = ({ widths = ["w-95","w-90","w-80","w-60"] }) => (
  <div className="lines">
    {widths.map((w, i) => <i key={i} className={w} />)}
  </div>
);

// Section header
const SectionHdr = ({ idx, label, right, h = "lg" }) => (
  <div className="section__hdr">
    <div>
      <span className="eyebrow">{idx} — {label}</span>
      {right === undefined ? null : null}
    </div>
    {right ? <div className="mono muted">{right}</div> : null}
  </div>
);

// Annotation bubble (toggleable via body[data-anno])
// position: {top, left, right, bottom} in px relative to nearest positioned ancestor (use .frame__body or .page)
const Anno = ({ children, top, left, right, bottom, arrow }) => (
  <div className="anno" style={{ top, left, right, bottom }}>
    {arrow ? <ArrowSvg {...arrow} /> : null}
    {children}
  </div>
);

const ArrowSvg = ({ dir = "down-left", w = 60, h = 40, top = 0, left = 0, right, bottom }) => {
  const path = (() => {
    switch (dir) {
      case "down-left":  return "M2,2 Q30,10 30,28 L30,32 M22,26 L30,34 L38,26";
      case "down-right": return `M${w-2},2 Q${w-30},10 ${w-30},28 L${w-30},32 M${w-38},26 L${w-30},34 L${w-22},26`;
      case "up-left":    return `M2,${h-2} Q30,${h-12} 30,4 M22,12 L30,2 L38,12`;
      case "up-right":   return `M${w-2},${h-2} Q${w-30},${h-12} ${w-30},4 M${w-38},12 L${w-30},2 L${w-22},12`;
      case "right":      return `M2,${h/2} L${w-8},${h/2} M${w-14},${h/2-6} L${w-2},${h/2} L${w-14},${h/2+6}`;
      case "left":       return `M${w-2},${h/2} L8,${h/2} M14,${h/2-6} L2,${h/2} L14,${h/2+6}`;
      default:           return "";
    }
  })();
  return (
    <svg width={w} height={h} style={{ top, left, right, bottom, position: "absolute" }}>
      <path d={path} stroke="#1f6feb" strokeWidth="1.4" fill="none" strokeLinecap="round" strokeLinejoin="round" />
    </svg>
  );
};

// Sketchy stamp (badge) for things like 9,56/10
const Stamp = ({ num, label, tilt = "l" }) => (
  <div className={`stamp ${tilt === "r" ? "tilt-r" : ""}`}>
    <div>
      <div className="num">{num}</div>
      <span className="meta">{label}</span>
    </div>
  </div>
);

// Pills for roles (UX/UI, Web Designer, etc)
const RolePills = ({ items = ["UX/UI", "Web Designer", "3D / Motion", "Dev Front", "IA Generativa"] }) => (
  <div className="pill-row">
    {items.map((t, i) => {
      const fills = ["fill-y", "fill-t", "fill-p", "fill-v", ""];
      return <span key={i} className={`pill ${fills[i % fills.length]}`}>{t}</span>;
    })}
  </div>
);

// Marquee with tool logos
const Marquee = ({ size = "default" }) => {
  const tools = [
    { name: "Figma",    glyph: "F" },
    { name: "Photoshop",glyph: "Ps" },
    { name: "Illustrator", glyph: "Ai" },
    { name: "Premiere", glyph: "Pr" },
    { name: "After Effects", glyph: "Ae" },
    { name: "Blender",  glyph: "B" },
    { name: "JavaScript", glyph: "JS" },
    { name: "HTML/CSS", glyph: "</>" },
    { name: "Claude",   glyph: "✦" },
    { name: "Builder.io", glyph: "B." },
  ];
  const cls = `marquee ${size === "compact" ? "compact" : size === "big" ? "big" : ""}`;
  return (
    <div className={cls}>
      <div className="marquee__track">
        {[...tools, ...tools].map((t, i) => (
          <div className="marquee__item" key={i}>
            <span className="logo">{t.glyph}</span>
            <span>{t.name}</span>
          </div>
        ))}
      </div>
    </div>
  );
};

// CTA buttons row
const Ctas = ({ primary = "Ver Projetos ↓", secondary = "Baixar CV (PDF) ↗", onPrimary, onSecondary }) => (
  <div className="flex gap-2 wrap">
    <button className="wf-btn primary" onClick={onPrimary}>{primary}</button>
    <button className="wf-btn" onClick={onSecondary}>{secondary}</button>
  </div>
);

// Frame for a "browser page"
const Frame = ({ url, children }) => (
  <div className="frame">
    <div className="frame__bar">
      <span className="dot" /><span className="dot" /><span className="dot" />
      <span className="url">filipeduarte.netlify.app/{url || ""}</span>
      <span className="muted">WIREFRAME</span>
    </div>
    <div className="frame__body">
      <div className="page" style={{ position: "relative" }}>
        {children}
      </div>
    </div>
  </div>
);

const PageSummary = ({ children }) => (
  <div className="page-summary">
    <b>Intenção</b>
    <span>{children}</span>
  </div>
);

Object.assign(window, {
  Nav, Footer, Box, Lines, SectionHdr, Anno, ArrowSvg,
  Stamp, RolePills, Marquee, Ctas, Frame, PageSummary
});


